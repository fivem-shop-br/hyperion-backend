version: '3.5'

volumes:
  data:

services:
  database:
    image: postgres
    ports:
      - 5432:5432
    environment:
      - POSTGRES_PASSWORD=value
    volumes:
      - data:/var/lib/postgresql/data