generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id            String   @id @default(cuid())
  email         String   @unique
  password      String
  name          String
  emailVerified Boolean?
  image         String?
  shops         Shop[]   @relation("UserInShop")
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
}

model Shop {
  id              String       @id @default(cuid())
  users           User[]       @relation("UserInShop")
  slug            String       @unique
  name            String
  description     String?
  logo            String?
  banner          String?
  favicon         String?
  primary_color   String?
  secondary_color String?
  domain          String
  plan            String
  categories      Categories[]
  createdAt       DateTime     @default(now())
  updatedAt       DateTime     @updatedAt
}

model Categories {
  id        String     @id @default(cuid())
  shop_id   String
  shop      Shop       @relation(fields: [shop_id], references: [id])
  name      String
  products  Products[]
  createdAt DateTime   @default(now())
  updatedAt DateTime   @updatedAt
}

model Products {
  id          String     @id @default(cuid())
  category_id String
  name        String
  image       String?
  price       Int
  categoty    Categories @relation(fields: [category_id], references: [id])
  createdAt   DateTime   @default(now())
  updatedAt   DateTime   @updatedAt
}
